{
  "name": "specscript",
  "version": "0.1.0",
  "description": "Language support for SpecScript files",
  "main": "dist/index.js",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "dev": "tsc -w",
    "test": "jest --watch",
    "test:ci": "jest --ci",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "cosmiconfig": "^9.0.0",
    "zod": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "@types/fs-extra": "^11.0.0",
    "@types/jest": "^29.5.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0"
  }
} 